/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package service;

import domain.Address;
import java.util.ArrayList;
import java.util.Collection;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;
import org.junit.Test;
import service.AddressService;
import service.AddressService;

/**
 *
 * @author xNlor
 */
public class AddressServiceTest {
   
    /*private AddressService serviceMap = new AddressService("Map");
    private AddressService serviceDB = new AddressService("DB");
    private Address a = new Address("Nollekensstraat",7,3061,"Leefdaal");
    @Test
    public void addAddressToMapWorks(){
        serviceMap.addAddress(a);
        Collection<Address> addresses = serviceMap.getAll();
        for(Address ad : addresses){
            System.out.println(ad.toString());
        }
        assertEquals(1,addresses.size());
        
        
    }
    @Test
    public void addAddressToDBWorks(){
        serviceDB.addAddress(a);
         Collection<Address> addresses = serviceDB.getAll();
         for(Address ad : addresses){
            System.out.println(ad.toString());
        }
        assertEquals(1,addresses.size());
    }

   
*/
    
   
}
